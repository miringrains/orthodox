import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Ensure routes are properly generated
  experimental: {
    // Enable if needed for route generation
  },
};

export default nextConfig;
